// TODO: add handler
import {
    EmbedHandlers,
    Embed
} from "../handler.js";

export const handler: EmbedHandlers['project'] = (event, options) => {
    const { sender, repository, action, project } = event

    return []
}